{
  "name": "bun-test",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "bun build index.ts --outfile=build/out.js --target=bun",
    "run-build": "bun build/out.js"
  },
  "dependencies": {
    "html-minifier": "^4.0.0"
  }
}
